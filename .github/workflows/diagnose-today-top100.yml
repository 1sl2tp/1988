name: Smoke production AI trends

on:
  push:
    branches: [diag-keyword-channel-scan]

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
      - name: Verify AI topics on production
        shell: bash
        run: |
          set -euo pipefail
          CHROME="$(command -v google-chrome || command -v google-chrome-stable || command -v chromium || command -v chromium-browser)"
          "$CHROME" --headless --no-sandbox --disable-gpu --disable-dev-shm-usage --remote-debugging-port=9222 about:blank >/tmp/chrome.log 2>&1 &
          PID=$!
          sleep 2
          node <<'NODE'
          const sleep=ms=>new Promise(r=>setTimeout(r,ms));
          async function connect(){
            let targets=[];
            for(let i=0;i<40;i++){
              try{targets=await (await fetch('http://127.0.0.1:9222/json')).json();if(targets.length)break}catch{}
              await sleep(250);
            }
            const target=targets.find(x=>x.type==='page');
            if(!target?.webSocketDebuggerUrl)throw new Error('no_cdp_target');
            const ws=new WebSocket(target.webSocketDebuggerUrl);
            let id=0;const pending=new Map();
            const send=(method,params={})=>new Promise((resolve,reject)=>{
              const msgId=++id;pending.set(msgId,{resolve,reject});ws.send(JSON.stringify({id:msgId,method,params}));
            });
            ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&pending.has(m.id)){const p=pending.get(m.id);pending.delete(m.id);m.error?p.reject(new Error(JSON.stringify(m.error))):p.resolve(m.result)}};
            await new Promise((resolve,reject)=>{ws.onopen=resolve;ws.onerror=reject});
            return {ws,send};
          }
          (async()=>{
            const {ws,send}=await connect();
            await send('Runtime.enable');
            await send('Page.enable');
            await send('Log.enable').catch(()=>{});
            await send('Page.navigate',{url:'https://yt.taphoa.xyz/?aitrend='+Date.now()});
            await sleep(18000);
            const out=await send('Runtime.evaluate',{
              expression:`(()=>{
                const chips=[...document.querySelectorAll('#trendTopics .trend-chip')].map(x=>x.textContent.trim()).filter(Boolean);
                const videos=document.querySelectorAll('#feed [data-video-id]').length;
                const hidden=document.querySelector('#trendTopics')?.hidden;
                const title=document.querySelector('#feedTitle')?.textContent?.trim()||'';
                return JSON.stringify({chips,videos,hidden,title});
              })()`,
              returnByValue:true
            });
            console.log('PROD_JSON='+out?.result?.value);
            ws.close();
          })().catch(e=>{console.error(e);process.exit(1)});
          NODE
          kill "$PID" 2>/dev/null || true
